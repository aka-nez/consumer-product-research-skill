# Repository Instructions

When the user asks to install or configure this project, read `INSTALL.md` and execute it end to end. Do not ask for a Firecrawl API key: the plugin uses Firecrawl's keyless hosted MCP endpoint. Do not hand-copy skill files or report success before the installed inventory shows one skill and one Firecrawl MCP server.
